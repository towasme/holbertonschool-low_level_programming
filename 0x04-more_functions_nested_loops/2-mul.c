#include "holberton.h"
/**
 *mul - multiplies two integers
 *@a: variable to be replaced
 *@b: vaariable to be multiplied
 *
 *Return: Always
 */
int mul(int a, int b)
{
	mul = (a * b);
	return(mul);
}
