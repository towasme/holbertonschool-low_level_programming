#include "holberton.h"
/**
 *_abs - computes the absolute value of an integer
 *@c: variable to be replaced
 *
 *Return: Always
 */
int _abs(int);
{
int r;
if(r < 0)
	{
		r = r*(-1);
		return (r);
	} else
		{return (r);
}
}
