#include <stdio.h>
/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int ch;
int CH;
for (ch = '0'; ch <= '8' ; ch++)
{
putchar(CH);
}
{
putchar(', ')
}
{
putchar(9)
}
putchar('\n');
return (0);
}
