#include "holberton.h"
/**
 *is_prime_number - prints characters
 *@n: var
 *Return: Always
 */
int is_prime_number(int n)
{
int a = 0;

	if (n <= 1)
		{
		return (0);
		}
	else if (n >= 2 && n / a == 1)
		{
		
		}
