#include "dog.h"
#include <stdio.h>
/**
 *free_dog - creates an array
 *@d: pointer
 *Return: Always
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	if (d != NULL)
	{
	free(d->name);
	free(d->owner);
	free(d);
	}
}
